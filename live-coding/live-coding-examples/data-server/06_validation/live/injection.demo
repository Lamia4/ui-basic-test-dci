# Javascript Injection:

userInput="<script>JAVASCRIPT</script>"

userInput -> SERVER 

---

SERVER:
   isValid(userInput);  => bspw. string?
   sanitize(userInput); => bspw. escapeHTML(userInput);

---

<h1>${userInput}</h1>

<
wird durch escapeHTML() bspw. zu:
&lt;
